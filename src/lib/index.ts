/**
 * Shared utilities and singleton clients.
 * Submodules:
 *   env.ts        — validated environment variables (zod)
 *   llm/          — Azure OpenAI client factory (WI-03)
 *   linkedin/     — LinkedIn API helpers and token crypto (WI-19)
 */
export {};
