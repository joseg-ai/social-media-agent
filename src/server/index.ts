/**
 * Server-side services and data-access helpers.
 * Next.js API routes and server actions import from here — never from src/db directly.
 * Each service module exposes typed functions over Drizzle queries.
 */
export {};
